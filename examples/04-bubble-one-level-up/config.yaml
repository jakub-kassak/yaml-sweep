# 04-bubble-one-level-up: !prod creates multiple instances of it's list element.
# In other words a list creates a boundary which doesn't propagate the product further.
list1:
  - outer_key: !prod [1, 2]
    list2: 
      - inner_key: !prod 
          - "a"
          - "b"
